// Update image paths from webp-images to optimized-images
// This is a simple find and replace for the image paths

export const updateImagePaths = () => {
  // This function can be called to update paths if needed
  // For now we'll just update the image URLs in the components manually
  console.log('Image paths should be updated from /webp-images/ to /optimized-images/small/');
};
